[project]
name = "apac-prototype"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11.9"
dependencies = [
    "streamlit>=1.33",
    "python-docx>=0.8.11",
    # transformers could be used for advanced summarisation, omitted here for simplicity
    # "transformers>=4.37",
]
